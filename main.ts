// xterm styling (loaded by Vite, not via Scala.js interop).
import "@xterm/xterm/css/xterm.css";

// Entrypoint: the Scala.js `app` module (with main-module initializer) is exposed by
// @scala-js/vite-plugin-scalajs at the virtual module "scalajs:main.js".
import "scalajs:main.js";
